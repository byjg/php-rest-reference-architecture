.idea/*
!.idea/runConfigurations
/Dockerfile

# API (PHP) — everything lives under api/
api/composer.lock
api/vendor
api/src/*.db
api/.phpunit.result.cache
api/config/.env

# Frontend (Vite)
html/node_modules
html/dist
html/.env

node_modules
.mysql
.phpunit.result.cache
TODO

thunder-tests

.secrets
.env
ignore.txt
/.claude/settings.local.json
/.bash_history

# Setup configuration (keep example only)
setup.json
